#include <functional>
//#include <iostream>
//#include <map>
//#include <string>


//#define KEY1 "key1"

#define Xint*   "asdf"

int main(){
    Xint*;

    /*
    std::map<std::string, std::string&> mymap{};
    //std::map<std::string, std::string&>::const_iterator myiter;

    std::string someStr="hiiiii";
    //mymap[KEY1] = someStr;
    //mymap[KEY1];
    mymap.insert({KEY1,someStr});
    std::cout<<"printinf someStr before the find change: '"<<someStr<<"'\n";

    //auto myiter = mymap.find(KEY1);
    //myiter->second = "changedd";

    mymap.at(KEY1)= "changedd";
    mymap.at("asdf");
    std::cout<<"printinf someStr AFTER the find change: '"<<someStr<<"'\n";

    //mymap[KEY1] = std::reference_wrapper<std::string>(someStr);

    */

    //std::map<std::string, std::string*> mymap1;
    //std::cout<<"asdf = "<<mymap1[KEY1]<<"\n";

    /*
    std::map<std::string, std::string*> mymap;
    std::map<std::string, std::string*>::const_iterator myiter;

    std::string someStr="hiiiii";
    mymap[KEY1] = &someStr;

    std::cout<<"printinf someStr before the find change: '"<<someStr<<"'\n";

    myiter = mymap.find(KEY1);
    *myiter->second = "changedd";

    std::cout<<"printinf someStr after the find change: '"<<someStr<<"'\n";
        */
}
